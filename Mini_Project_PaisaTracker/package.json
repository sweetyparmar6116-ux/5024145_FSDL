{
  "name": "expensetracker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --names \"CLIENT,SERVER\" --prefix-colors \"cyan,green\" \"vite\" \"npm run dev --prefix backend\"",
    "dev:client": "vite",
    "dev:server": "npm run dev --prefix backend",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.10",
    "vite": "^8.0.9"
  }
}
